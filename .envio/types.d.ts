/**
 * This file is generated by HyperIndex codegen from config.yaml and schema.graphql.
 * Do not edit manually.
 * If your project's types look out of date, run `envio codegen`
 * (or your package manager's `codegen` script, e.g. `pnpm codegen`).
 */

import type { Address, BigDecimal, SingleOrMultiple } from "envio";

type EvmBlock = {
  /** The number field. */
  readonly number: number;
  /** The timestamp field. */
  readonly timestamp: number;
  /** The hash field. */
  readonly hash: string;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       block_fields:
   *         - parentHash
   * ```
   */
  readonly parentHash: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       block_fields:
   *         - nonce
   * ```
   */
  readonly nonce: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       block_fields:
   *         - sha3Uncles
   * ```
   */
  readonly sha3Uncles: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       block_fields:
   *         - logsBloom
   * ```
   */
  readonly logsBloom: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       block_fields:
   *         - transactionsRoot
   * ```
   */
  readonly transactionsRoot: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       block_fields:
   *         - stateRoot
   * ```
   */
  readonly stateRoot: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       block_fields:
   *         - receiptsRoot
   * ```
   */
  readonly receiptsRoot: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       block_fields:
   *         - miner
   * ```
   */
  readonly miner: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       block_fields:
   *         - difficulty
   * ```
   */
  readonly difficulty: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       block_fields:
   *         - totalDifficulty
   * ```
   */
  readonly totalDifficulty: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       block_fields:
   *         - extraData
   * ```
   */
  readonly extraData: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       block_fields:
   *         - size
   * ```
   */
  readonly size: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       block_fields:
   *         - gasLimit
   * ```
   */
  readonly gasLimit: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       block_fields:
   *         - gasUsed
   * ```
   */
  readonly gasUsed: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       block_fields:
   *         - uncles
   * ```
   */
  readonly uncles: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       block_fields:
   *         - baseFeePerGas
   * ```
   */
  readonly baseFeePerGas: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       block_fields:
   *         - blobGasUsed
   * ```
   */
  readonly blobGasUsed: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       block_fields:
   *         - excessBlobGas
   * ```
   */
  readonly excessBlobGas: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       block_fields:
   *         - parentBeaconBlockRoot
   * ```
   */
  readonly parentBeaconBlockRoot: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       block_fields:
   *         - withdrawalsRoot
   * ```
   */
  readonly withdrawalsRoot: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       block_fields:
   *         - l1BlockNumber
   * ```
   */
  readonly l1BlockNumber: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       block_fields:
   *         - sendCount
   * ```
   */
  readonly sendCount: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       block_fields:
   *         - sendRoot
   * ```
   */
  readonly sendRoot: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       block_fields:
   *         - mixHash
   * ```
   */
  readonly mixHash: never;
};
type EvmTransaction = {
  /** The transactionIndex field. */
  readonly transactionIndex: number;
  /** The hash field. */
  readonly hash: string;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       transaction_fields:
   *         - from
   * ```
   */
  readonly from: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       transaction_fields:
   *         - to
   * ```
   */
  readonly to: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       transaction_fields:
   *         - gas
   * ```
   */
  readonly gas: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       transaction_fields:
   *         - gasPrice
   * ```
   */
  readonly gasPrice: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       transaction_fields:
   *         - maxPriorityFeePerGas
   * ```
   */
  readonly maxPriorityFeePerGas: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       transaction_fields:
   *         - maxFeePerGas
   * ```
   */
  readonly maxFeePerGas: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       transaction_fields:
   *         - cumulativeGasUsed
   * ```
   */
  readonly cumulativeGasUsed: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       transaction_fields:
   *         - effectiveGasPrice
   * ```
   */
  readonly effectiveGasPrice: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       transaction_fields:
   *         - gasUsed
   * ```
   */
  readonly gasUsed: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       transaction_fields:
   *         - input
   * ```
   */
  readonly input: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       transaction_fields:
   *         - nonce
   * ```
   */
  readonly nonce: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       transaction_fields:
   *         - value
   * ```
   */
  readonly value: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       transaction_fields:
   *         - v
   * ```
   */
  readonly v: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       transaction_fields:
   *         - r
   * ```
   */
  readonly r: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       transaction_fields:
   *         - s
   * ```
   */
  readonly s: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       transaction_fields:
   *         - contractAddress
   * ```
   */
  readonly contractAddress: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       transaction_fields:
   *         - logsBloom
   * ```
   */
  readonly logsBloom: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       transaction_fields:
   *         - root
   * ```
   */
  readonly root: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       transaction_fields:
   *         - status
   * ```
   */
  readonly status: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       transaction_fields:
   *         - yParity
   * ```
   */
  readonly yParity: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       transaction_fields:
   *         - accessList
   * ```
   */
  readonly accessList: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       transaction_fields:
   *         - maxFeePerBlobGas
   * ```
   */
  readonly maxFeePerBlobGas: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       transaction_fields:
   *         - blobVersionedHashes
   * ```
   */
  readonly blobVersionedHashes: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       transaction_fields:
   *         - type
   * ```
   */
  readonly type: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       transaction_fields:
   *         - l1Fee
   * ```
   */
  readonly l1Fee: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       transaction_fields:
   *         - l1GasPrice
   * ```
   */
  readonly l1GasPrice: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       transaction_fields:
   *         - l1GasUsed
   * ```
   */
  readonly l1GasUsed: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       transaction_fields:
   *         - l1FeeScalar
   * ```
   */
  readonly l1FeeScalar: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       transaction_fields:
   *         - gasUsedForL1
   * ```
   */
  readonly gasUsedForL1: never;
  /**
   * @deprecated Not selected for this event. To enable, add to config.yaml:
   * ```yaml
   * events:
   *   - event: global
   *     field_selection:
   *       transaction_fields:
   *         - authorizationList
   * ```
   */
  readonly authorizationList: never;
};
declare namespace FuelTypes {}
type Enums = {
  "LimitOrderProtocol": "ZeroEx" | "OneInch";
  "MarketplacePoolTransactionType": "DEPOSIT" | "WITHDRAW" | "REWARD";
  "PropertyStakingPoolTransactionType": "DEPOSIT" | "WITHDRAW" | "REWARD";
  "PropertyStakingPoolType": "COMMUNITY" | "ISSUER" | "RWA" | "LANDHIVE";
  "StakingPoolTransactionType": "DEPOSIT" | "WITHDRAW" | "REWARD";
};
type Entities = {
  "AssetPair": {
    readonly "id": string;
    readonly "latestPrice": number;
    readonly "latestPriceBI": bigint;
    readonly "updatedAt": number;
    readonly "latestAggregatorAddress": string;
  };
  "AssetPairPrice": {
    readonly "id": string;
    readonly "assetPair_id": string;
    readonly "timestamp": number;
    readonly "hourStartTimestamp": number;
    readonly "dayStartTimestamp": number;
    readonly "blockNumber": number;
    readonly "priceBI": bigint;
    readonly "price": number;
  };
  "CertifiedPartner": {
    readonly "id": string;
    readonly "chainId": number;
    readonly "name": string;
  };
  "Global": {
    readonly "id": string;
    readonly "activePropertiesCount": number;
    readonly "activePropertiesTotalValuation": bigint;
    readonly "activePropertiesCountryCount": number;
  };
  "GlobalRecord": {
    readonly "id": string;
    readonly "hourStartTimestamp": number;
    readonly "dayStartTimestamp": number;
    readonly "activePropertiesCount": number;
    readonly "activePropertiesTotalValuation": bigint;
    readonly "activePropertiesCountryCount": number;
  };
  "MainSaleInvestment": {
    readonly "id": string;
    readonly "chainId": number;
    readonly "transactionHash": string;
    readonly "mainSaleOffering_id": string;
    readonly "propertyToken_id": string;
    readonly "wallet_id": string;
    readonly "investmentToken": string;
    readonly "amountInvested": bigint;
    readonly "amountReceived": bigint;
    readonly "blockTimestamp": number;
  };
  "MainSaleOffering": {
    readonly "id": string;
    readonly "chainId": number;
    readonly "offering_id": string;
    readonly "startDateTimestamp": number;
    readonly "endDateTimestamp": number;
    readonly "maxInvestment": bigint;
    readonly "minInvestment": bigint;
    readonly "mintedAmount": bigint;
    readonly "pricePerToken": bigint;
    readonly "assetPriceIdentifier": string;
    readonly "softCap": bigint;
    readonly "hardCap": bigint;
    readonly "collector": string;
    readonly "feeCollector": string;
    readonly "fee": bigint;
    readonly "investmentTokens": readonly string[];
    readonly "status": string;
  };
  "MarketplacePool": {
    readonly "id": string;
    readonly "chainId": number;
    readonly "contractAddress": string;
    readonly "capped": boolean;
    readonly "certifiedPartnerCollateralAmount": bigint;
    readonly "certifiedPartnerCanWithdraw": boolean;
    readonly "certifiedPartnerIdentifier": string;
    readonly "certifiedPartnerUrl": string;
    readonly "certifiedPartnerWallet": string;
    readonly "certifiedPartnerPoolPosition_id": string;
    readonly "bsWallet": string;
    readonly "duration": number;
    readonly "lockPeriod": number;
    readonly "lockedUntil": number;
    readonly "stakedAmount": bigint;
    readonly "totalDepositAmount": bigint;
    readonly "totalWithdrawAmount": bigint;
    readonly "startMaxPledge": bigint;
    readonly "startTime": number;
    readonly "tokenName": string;
    readonly "tokenSymbol": string;
    readonly "totalRewardsAdded": bigint;
    readonly "totalRewardsClaimed": bigint;
    readonly "vAmount": bigint;
    readonly "isCpCollateralLiquidated": boolean;
    readonly "cpCollateralLiquidatedAt": number;
  };
  "MarketplacePoolPosition": {
    readonly "id": string;
    readonly "chainId": number;
    readonly "pool_id": string;
    readonly "wallet_id": string;
    readonly "stakedAmount": bigint;
    readonly "vAmount": bigint;
    readonly "rewards": bigint;
  };
  "MarketplacePoolRecord": {
    readonly "id": string;
    readonly "chainId": number;
    readonly "hourStartTimestamp": number;
    readonly "dayStartTimestamp": number;
    readonly "pool_id": string;
    readonly "stakedAmount": bigint;
    readonly "totalDepositAmount": bigint;
    readonly "totalWithdrawAmount": bigint;
    readonly "totalRewardsAdded": bigint;
    readonly "totalRewardsClaimed": bigint;
    readonly "vAmount": bigint;
  };
  "MarketplacePoolTransaction": {
    readonly "id": string;
    readonly "chainId": number;
    readonly "pool_id": string;
    readonly "transactionType": Enums["MarketplacePoolTransactionType"];
    readonly "transactionHash": string;
    readonly "blockNumber": number;
    readonly "blockTimestamp": number;
    readonly "wallet_id": string;
    readonly "amount": bigint;
    readonly "issuedAmount": bigint | undefined;
    readonly "reward": bigint | undefined;
    readonly "ratioAtTransaction": BigDecimal;
    readonly "dayStartTimestamp": number;
  };
  "OceanpointTokenInformation": {
    readonly "id": string;
    readonly "chainId": number;
    readonly "propertyToken_id": string;
    readonly "propertyStakingPool_id": string;
    readonly "propertyStakingPoolType": Enums["PropertyStakingPoolType"];
    readonly "valuation": bigint;
    readonly "valuationFrom": string;
    readonly "valuePerBSPT": bigint;
    readonly "apy": bigint;
  };
  "PresaleOffering": {
    readonly "id": string;
    readonly "chainId": number;
    readonly "offering_id": string;
    readonly "startDateTimestamp": number;
    readonly "endDateTimestamp": number;
    readonly "maxInvestment": bigint;
    readonly "minInvestment": bigint;
    readonly "pricePerToken": bigint;
    readonly "softCap": bigint;
    readonly "hardCap": bigint;
    readonly "totalMintedAmount": bigint;
    readonly "status": string;
  };
  "PropertyStakingPool": {
    readonly "id": string;
    readonly "chainId": number;
    readonly "issuedAmount": bigint;
    readonly "currentAmount": bigint;
    readonly "totalRewards": bigint;
    readonly "totalRewardsPaid": bigint;
    readonly "currentRewards": bigint;
    readonly "tvl": bigint;
  };
  "PropertyStakingPoolPosition": {
    readonly "id": string;
    readonly "chainId": number;
    readonly "pool_id": string;
    readonly "tokenInformation_id": string;
    readonly "wallet_id": string;
    readonly "totalIssuedAmount": bigint;
    readonly "totalStakedAmount": bigint;
    readonly "totalStakedValue": bigint;
  };
  "PropertyStakingPoolRecord": {
    readonly "id": string;
    readonly "chainId": number;
    readonly "hourStartTimestamp": number;
    readonly "dayStartTimestamp": number;
    readonly "issuedAmount": bigint;
    readonly "currentAmount": bigint;
    readonly "totalRewards": bigint;
    readonly "totalRewardsPaid": bigint;
    readonly "currentRewards": bigint;
    readonly "pool_id": string;
    readonly "tvl": bigint;
  };
  "PropertyStakingPoolTransaction": {
    readonly "id": string;
    readonly "chainId": number;
    readonly "pool_id": string;
    readonly "transactionType": Enums["PropertyStakingPoolTransactionType"];
    readonly "transactionHash": string;
    readonly "blockNumber": number;
    readonly "blockTimestamp": number;
    readonly "wallet_id": string;
    readonly "amount": bigint;
    readonly "tokenAddress": string;
    readonly "issuedAmount": bigint | undefined;
    readonly "rewardToUser": bigint | undefined;
    readonly "rewardToFeeReceiver": bigint | undefined;
    readonly "dayStartTimestamp": number;
  };
  "PropertyToken": {
    readonly "id": string;
    readonly "chainId": number;
    readonly "contractAddress": string;
    readonly "name": string;
    readonly "symbol": string;
    readonly "totalSupply": bigint;
    readonly "totalBurnt": bigint;
    readonly "totalTransfers": number;
    readonly "totalHolders": number;
    readonly "tokenValuation": bigint;
    readonly "propertyValuation": bigint;
    readonly "propertyValuationUpdateTimestamp": number;
    readonly "propertyValuationCurrency": string;
    readonly "certifiedPartnerWallet_id": string;
    readonly "streetLocation": string;
    readonly "geoLocation": string;
    readonly "lat": number;
    readonly "lng": number;
    readonly "countryCode": string;
    readonly "parcelNumber": string;
    readonly "kadastralMunicipality": string;
    readonly "buildingPart": number;
    readonly "propertyType": string;
    readonly "ipfs": string;
    readonly "createdAt": number;
    readonly "createdAtBlock": number;
    readonly "creationTransaction": string;
    readonly "totalPropertyTokenTraded": bigint;
    readonly "totalValueTraded": bigint;
    readonly "certifiedPartner_id": string;
    readonly "latestOffering_id": string;
    readonly "offeringV2_id": string | undefined;
    readonly "apy": number;
    readonly "currentYearApy": number;
    readonly "weightedNAVDeviation": number;
  };
  "PropertyTokenCapitalStack": {
    readonly "id": string;
    readonly "chainId": number;
    readonly "property_id": string;
    readonly "timestamp": number;
    readonly "tokenizationAmount": bigint;
    readonly "commonEquity": bigint;
    readonly "preferredEquity": bigint;
    readonly "mezzanine": bigint;
    readonly "juniorDebt": bigint;
    readonly "seniorDebt": bigint;
  };
  "PropertyTokenHolder": {
    readonly "id": string;
    readonly "chainId": number;
    readonly "amount": bigint;
    readonly "token_id": string;
    readonly "wallet_id": string;
  };
  "PropertyTokenInvestment": {
    readonly "id": string;
    readonly "chainId": number;
    readonly "transactionHash": string;
    readonly "propertyToken_id": string;
    readonly "wallet_id": string;
    readonly "investmentToken": string;
    readonly "amountInvested": bigint;
    readonly "amountReceived": bigint;
    readonly "blockTimestamp": number;
  };
  "PropertyTokenOffering": {
    readonly "id": string;
    readonly "chainId": number;
    readonly "property_id": string;
    readonly "price": number;
    readonly "softCap": bigint;
    readonly "presaleMaxInvestment": bigint;
    readonly "presaleMinInvestment": bigint;
    readonly "maxInvestment": bigint;
    readonly "minInvestment": bigint;
    readonly "presaleStartTimestamp": number;
    readonly "presaleEndTimestamp": number;
    readonly "saleStartTimestamp": number;
    readonly "saleEndTimestamp": number;
    readonly "investmentToken": string;
    readonly "collector": string;
    readonly "feeCollector": string;
    readonly "fee": bigint;
    readonly "status": string;
  };
  "PropertyTokenOfferingV2": {
    readonly "id": string;
    readonly "chainId": number;
    readonly "property_id": string;
  };
  "PropertyTokenRecord": {
    readonly "id": string;
    readonly "chainId": number;
    readonly "hourStartTimestamp": number;
    readonly "dayStartTimestamp": number;
    readonly "totalSupply": bigint;
    readonly "totalBurnt": bigint;
    readonly "totalTransfers": number;
    readonly "totalHolders": number;
    readonly "tokenValuation": bigint;
    readonly "propertyValuation": bigint;
    readonly "propertyToken_id": string;
    readonly "countryCode": string;
  };
  "PropertyTokenRevenue": {
    readonly "id": string;
    readonly "chainId": number;
    readonly "contractAddress": string;
    readonly "propertyToken_id": string;
    readonly "wallet_id": string;
    readonly "pendingRevenue": bigint;
    readonly "claimedRevenue": bigint;
  };
  "PropertyTokenRevenueClaim": {
    readonly "id": string;
    readonly "chainId": number;
    readonly "contractAddress": string;
    readonly "transactionHash": string;
    readonly "blockNumber": number;
    readonly "blockTimestamp": number;
    readonly "propertyToken_id": string;
    readonly "wallet_id": string;
    readonly "amount": bigint;
  };
  "PropertyTokenRevenueDistribution": {
    readonly "id": string;
    readonly "blockTimestamp": number;
    readonly "chainId": number;
    readonly "contractAddress": string;
    readonly "propertyToken_id": string;
    readonly "amounts": readonly bigint[];
    readonly "totalAmount": bigint;
    readonly "totalSupply": bigint;
    readonly "propertyValuation": bigint;
    readonly "users": readonly string[];
    readonly "fromTime": bigint;
    readonly "toTime": bigint;
  };
  "PropertyTokenTrade": {
    readonly "id": string;
    readonly "chainID": number;
    readonly "transactionHash": string;
    readonly "blockNumber": number;
    readonly "blockTimestamp": number;
    readonly "propertyToken_id": string;
    readonly "maker_id": string;
    readonly "taker_id": string;
    readonly "orderHash": string;
    readonly "makerToken": string;
    readonly "takerToken": string;
    readonly "takerTokenFilledAmount": bigint;
    readonly "makerTokenFilledAmount": bigint;
    readonly "propertyValuation": bigint;
    readonly "protocol": Enums["LimitOrderProtocol"];
    readonly "referralCode": string | undefined;
  };
  "PropertyTokenTradeCounts": {
    readonly "id": string;
    readonly "chainId": number;
    readonly "takerCount": number;
    readonly "makerCount": number;
  };
  "PropertyTokenTransfer": {
    readonly "id": string;
    readonly "chainId": number;
    readonly "amount": bigint;
    readonly "blockHash": string;
    readonly "blockNumber": number;
    readonly "blockTimestamp": number;
    readonly "from_id": string;
    readonly "logIndex": number;
    readonly "propertyToken_id": string;
    readonly "to_id": string;
    readonly "transactionHash": string;
    readonly "transactionIndex": number;
  };
  "Referral": {
    readonly "id": string;
    readonly "wallet_id": string;
    readonly "referrer_id": string;
    readonly "marketplace_id": string;
    readonly "firstOrderTrade_id": string;
    readonly "createdAt": number;
  };
  "Reward": {
    readonly "id": string;
    readonly "chainId": number;
  };
  "StakingPool": {
    readonly "id": string;
    readonly "chainId": number;
    readonly "issuedAmount": bigint;
    readonly "stakedAmount": bigint;
    readonly "ratio": BigDecimal;
    readonly "currentAmount": bigint;
    readonly "totalDepositAmount": bigint;
    readonly "totalRewards": bigint;
    readonly "totalWithdrawAmount": bigint;
  };
  "StakingPoolPosition": {
    readonly "id": string;
    readonly "chainId": number;
    readonly "pool_id": string;
    readonly "wallet_id": string;
    readonly "stakedAmount": bigint;
    readonly "issuedAmount": bigint;
    readonly "lockedUntil": number;
  };
  "StakingPoolPositionRecord": {
    readonly "id": string;
    readonly "chainId": number;
    readonly "pool_id": string;
    readonly "walletAddress": string;
    readonly "issuedAmount": bigint;
    readonly "ratio": BigDecimal;
    readonly "blockTimestamp": number;
    readonly "blockNumber": number;
    readonly "transactionHash": string;
  };
  "StakingPoolRecord": {
    readonly "id": string;
    readonly "chainId": number;
    readonly "hourStartTimestamp": number;
    readonly "dayStartTimestamp": number;
    readonly "issuedAmount": bigint;
    readonly "stakedAmount": bigint;
    readonly "ratio": BigDecimal;
    readonly "currentAmount": bigint;
    readonly "totalDepositAmount": bigint;
    readonly "totalRewards": bigint;
    readonly "totalWithdrawAmount": bigint;
    readonly "pool_id": string;
  };
  "StakingPoolTransaction": {
    readonly "id": string;
    readonly "chainId": number;
    readonly "pool_id": string;
    readonly "transactionType": Enums["StakingPoolTransactionType"];
    readonly "transactionHash": string;
    readonly "blockNumber": number;
    readonly "blockTimestamp": number;
    readonly "wallet_id": string;
    readonly "amount": bigint;
    readonly "issuedAmount": bigint | undefined;
    readonly "dayStartTimestamp": number;
  };
  "Token": {
    readonly "id": string;
    readonly "chainId": number;
    readonly "contractAddress": string;
    readonly "name": string;
    readonly "symbol": string;
    readonly "totalSupply": bigint;
    readonly "totalBurnt": bigint;
    readonly "totalTransfers": number;
    readonly "totalHolders": number;
    readonly "marketcap": number;
    readonly "marketcapBI": bigint;
  };
  "TokenDeposit": {
    readonly "id": string;
    readonly "stakingPoolPosition_id": string;
    readonly "tokenInformation_id": string;
    readonly "issuedAmount": bigint;
    readonly "stakedAmount": bigint;
    readonly "valuePerBSPT": bigint;
    readonly "lockedUntil": number;
    readonly "stakedValue": bigint;
  };
  "TokenHolder": {
    readonly "id": string;
    readonly "chainId": number;
    readonly "amount": bigint;
    readonly "token_id": string;
  };
  "TokenHolderRecord": {
    readonly "id": string;
    readonly "chainId": number;
    readonly "token_id": string;
    readonly "walletAddress": string;
    readonly "amount": bigint;
    readonly "blockTimestamp": number;
    readonly "blockNumber": number;
    readonly "transactionHash": string;
  };
  "TokenRecord": {
    readonly "id": string;
    readonly "chainId": number;
    readonly "hourStartTimestamp": number;
    readonly "dayStartTimestamp": number;
    readonly "totalSupply": bigint;
    readonly "totalBurnt": bigint;
    readonly "totalTransfers": number;
    readonly "totalHolders": number;
    readonly "marketcap": number;
    readonly "marketcapBI": bigint;
    readonly "token_id": string;
  };
  "UniswapV2Pool": {
    readonly "id": string;
    readonly "assetPairId": string;
    readonly "totalSupply": bigint;
    readonly "reserve0": bigint;
    readonly "reserve1": bigint;
  };
  "User": {
    readonly "id": string;
    readonly "chainId": number;
  };
  "UserCertifiedPartner": {
    readonly "id": string;
    readonly "chainId": number;
    readonly "user_id": string;
    readonly "certifiedPartner_id": string;
  };
  "Wallet": {
    readonly "id": string;
    readonly "address": string;
    readonly "chainId": number;
    readonly "user_id": string | undefined;
    readonly "certifiedPartner_id": string | undefined;
  };
};

declare module "envio" {
  interface Global {
    config: {
      evm: { chains: {
  "ethereumMainnet": { id: 1 };
      }; contracts: {
  "PropertyTokenOffering": {
    "InitialOffer": {
      /** The name of the event. */
      readonly eventName: "InitialOffer";
      /** The name of the contract that emitted this event. */
      readonly contractName: "PropertyTokenOffering";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly property: Address; readonly pricePerToken: bigint; readonly presaleMaxInvestment: bigint; readonly presaleMinInvestment: bigint; readonly maxInvestment: bigint; readonly minInvestment: bigint; readonly softCap: bigint; readonly presaleStart: bigint; readonly presaleEnd: bigint; readonly saleStart: bigint; readonly saleEnd: bigint; readonly investmentCurrency: Address; readonly collector: Address; readonly feeCollector: Address; readonly fee: bigint };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
    "Invested": {
      /** The name of the event. */
      readonly eventName: "Invested";
      /** The name of the contract that emitted this event. */
      readonly contractName: "PropertyTokenOffering";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly property: Address; readonly wallet: Address; readonly investmentToken: Address; readonly amountInvested: bigint; readonly amountReceived: bigint };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
    "InitialOfferingCanceled": {
      /** The name of the event. */
      readonly eventName: "InitialOfferingCanceled";
      /** The name of the contract that emitted this event. */
      readonly contractName: "PropertyTokenOffering";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly property: Address };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
    "ClaimInvestment": {
      /** The name of the event. */
      readonly eventName: "ClaimInvestment";
      /** The name of the contract that emitted this event. */
      readonly contractName: "PropertyTokenOffering";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly property: Address; readonly amount: bigint; readonly feeAmount: bigint };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
    "ReturnedPresaleInvestment": {
      /** The name of the event. */
      readonly eventName: "ReturnedPresaleInvestment";
      /** The name of the contract that emitted this event. */
      readonly contractName: "PropertyTokenOffering";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly property: Address; readonly wallet: Address; readonly amount: bigint };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
    "ReturnedInvestment": {
      /** The name of the event. */
      readonly eventName: "ReturnedInvestment";
      /** The name of the contract that emitted this event. */
      readonly contractName: "PropertyTokenOffering";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly property: Address; readonly wallet: Address; readonly amount: bigint };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
  };
  "PropertyRegistry": {
    "IPFSHashChanged": {
      /** The name of the event. */
      readonly eventName: "IPFSHashChanged";
      /** The name of the contract that emitted this event. */
      readonly contractName: "PropertyRegistry";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly property: Address; readonly newIPFSHash: string };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
    "NameAndSymbolChange": {
      /** The name of the event. */
      readonly eventName: "NameAndSymbolChange";
      /** The name of the contract that emitted this event. */
      readonly contractName: "PropertyRegistry";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly property: Address; readonly newName: string; readonly newSymbol: string };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
    "PropertyBasicInfoChanged": {
      /** The name of the event. */
      readonly eventName: "PropertyBasicInfoChanged";
      /** The name of the contract that emitted this event. */
      readonly contractName: "PropertyRegistry";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly property: Address; readonly streetLocation: string; readonly geoLocation: string; readonly propertyValuationCurrency: string; readonly propertyValuation: bigint };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
    "PropertyInfoAdded": {
      /** The name of the event. */
      readonly eventName: "PropertyInfoAdded";
      /** The name of the contract that emitted this event. */
      readonly contractName: "PropertyRegistry";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly property: Address; readonly propertyType: string; readonly kadastralMunicipality: string; readonly parcelNumber: string; readonly ID: string; readonly buildingPart: bigint };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
    "PropertyInfoChanged": {
      /** The name of the event. */
      readonly eventName: "PropertyInfoChanged";
      /** The name of the contract that emitted this event. */
      readonly contractName: "PropertyRegistry";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly property: Address; readonly idProp: bigint; readonly propertyType: string; readonly kadastralMunicipality: string; readonly parcelNumber: string; readonly ID: string; readonly buildingPart: bigint };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
    "PropertyValuationChange": {
      /** The name of the event. */
      readonly eventName: "PropertyValuationChange";
      /** The name of the contract that emitted this event. */
      readonly contractName: "PropertyRegistry";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly property: Address; readonly newValuationProperty: bigint };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
    "TokenValuationChange": {
      /** The name of the event. */
      readonly eventName: "TokenValuationChange";
      /** The name of the contract that emitted this event. */
      readonly contractName: "PropertyRegistry";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly property: Address; readonly newTokenValuation: bigint };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
  };
  "OneInchPostInteraction": {
    "PostInteractionOrderFilled": {
      /** The name of the event. */
      readonly eventName: "PostInteractionOrderFilled";
      /** The name of the contract that emitted this event. */
      readonly contractName: "OneInchPostInteraction";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly orderHash: string; readonly taker: Address; readonly makingAmount: bigint; readonly remainingMakingAmount: bigint; readonly order: { readonly salt: bigint; readonly maker: bigint; readonly receiver: bigint; readonly makerAsset: bigint; readonly takerAsset: bigint; readonly makingAmount: bigint; readonly takingAmount: bigint; readonly makerTraits: bigint }; readonly extension: string; readonly takingAmount: bigint; readonly extraData: string };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
  };
  "PropertyTokenOfferingV2": {
    "MainSaleAdded": {
      /** The name of the event. */
      readonly eventName: "MainSaleAdded";
      /** The name of the contract that emitted this event. */
      readonly contractName: "PropertyTokenOfferingV2";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly property: Address; readonly mainSaleId: bigint; readonly startDate: bigint; readonly endDate: bigint; readonly maxInvestment: bigint; readonly minInvestment: bigint; readonly pricePerToken: bigint; readonly assetPriceIdentifier: string; readonly softCap: bigint; readonly hardCap: bigint; readonly collector: Address; readonly feeCollector: Address; readonly fee: bigint; readonly investmentTokens: readonly Address[] };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
    "PresaleAdded": {
      /** The name of the event. */
      readonly eventName: "PresaleAdded";
      /** The name of the contract that emitted this event. */
      readonly contractName: "PropertyTokenOfferingV2";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly property: Address; readonly presaleId: bigint; readonly startDate: bigint; readonly endDate: bigint; readonly softCap: bigint; readonly hardCap: bigint; readonly pricePerToken: bigint; readonly maxInvestment: bigint; readonly minInvestment: bigint };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
    "Invested": {
      /** The name of the event. */
      readonly eventName: "Invested";
      /** The name of the contract that emitted this event. */
      readonly contractName: "PropertyTokenOfferingV2";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly property: Address; readonly wallet: Address; readonly mainSaleId: bigint; readonly investmentToken: Address; readonly amountInvested: bigint; readonly amountReceived: bigint };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
    "PresaleTokensMinted": {
      /** The name of the event. */
      readonly eventName: "PresaleTokensMinted";
      /** The name of the contract that emitted this event. */
      readonly contractName: "PropertyTokenOfferingV2";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly property: Address; readonly presaleId: bigint; readonly totalMintedAmount: bigint; readonly wallets: readonly Address[]; readonly amounts: readonly bigint[] };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
    "ClaimInvestment": {
      /** The name of the event. */
      readonly eventName: "ClaimInvestment";
      /** The name of the contract that emitted this event. */
      readonly contractName: "PropertyTokenOfferingV2";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly property: Address; readonly mainSaleId: bigint; readonly investmentToken: Address; readonly amount: bigint; readonly feeAmount: bigint };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
    "MainSaleCanceled": {
      /** The name of the event. */
      readonly eventName: "MainSaleCanceled";
      /** The name of the contract that emitted this event. */
      readonly contractName: "PropertyTokenOfferingV2";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly property: Address; readonly mainSaleId: bigint };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
    "PresaleCanceled": {
      /** The name of the event. */
      readonly eventName: "PresaleCanceled";
      /** The name of the contract that emitted this event. */
      readonly contractName: "PropertyTokenOfferingV2";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly property: Address; readonly presaleId: bigint };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
    "MainSaleInvestmentsRefunded": {
      /** The name of the event. */
      readonly eventName: "MainSaleInvestmentsRefunded";
      /** The name of the contract that emitted this event. */
      readonly contractName: "PropertyTokenOfferingV2";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly property: Address; readonly mainSaleId: bigint; readonly wallets: readonly Address[] };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
    "PresaleInvestmentsRefunded": {
      /** The name of the event. */
      readonly eventName: "PresaleInvestmentsRefunded";
      /** The name of the contract that emitted this event. */
      readonly contractName: "PropertyTokenOfferingV2";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly property: Address; readonly presaleId: bigint; readonly wallets: readonly Address[] };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
  };
  "PriceDataFeed": {
    "AnswerUpdated": {
      /** The name of the event. */
      readonly eventName: "AnswerUpdated";
      /** The name of the contract that emitted this event. */
      readonly contractName: "PriceDataFeed";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly current: bigint; readonly roundId: bigint; readonly updatedAt: bigint };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
  };
  "MarketplacePool": {
    "CPInitialized": {
      /** The name of the event. */
      readonly eventName: "CPInitialized";
      /** The name of the contract that emitted this event. */
      readonly contractName: "MarketplacePool";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly amount: bigint; readonly lockPeriod: bigint };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
    "Deposit": {
      /** The name of the event. */
      readonly eventName: "Deposit";
      /** The name of the contract that emitted this event. */
      readonly contractName: "MarketplacePool";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly owner: Address; readonly inAmount: bigint; readonly outAmount: bigint };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
    "Withdraw": {
      /** The name of the event. */
      readonly eventName: "Withdraw";
      /** The name of the contract that emitted this event. */
      readonly contractName: "MarketplacePool";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly owner: Address; readonly inAmount: bigint; readonly outAmount: bigint; readonly reward: bigint };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
    "CPCanWithdraw": {
      /** The name of the event. */
      readonly eventName: "CPCanWithdraw";
      /** The name of the contract that emitted this event. */
      readonly contractName: "MarketplacePool";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: undefined;
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
    "Capped": {
      /** The name of the event. */
      readonly eventName: "Capped";
      /** The name of the contract that emitted this event. */
      readonly contractName: "MarketplacePool";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly lockEnd: bigint };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
    "Reward": {
      /** The name of the event. */
      readonly eventName: "Reward";
      /** The name of the contract that emitted this event. */
      readonly contractName: "MarketplacePool";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly from: Address; readonly amount: bigint };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
    "LockExtended": {
      /** The name of the event. */
      readonly eventName: "LockExtended";
      /** The name of the contract that emitted this event. */
      readonly contractName: "MarketplacePool";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly extension: bigint };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
    "LiquidateCPCollateral": {
      /** The name of the event. */
      readonly eventName: "LiquidateCPCollateral";
      /** The name of the contract that emitted this event. */
      readonly contractName: "MarketplacePool";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly amount: bigint };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
    "PoolCampaignConfigured": {
      /** The name of the event. */
      readonly eventName: "PoolCampaignConfigured";
      /** The name of the contract that emitted this event. */
      readonly contractName: "MarketplacePool";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly startTime: bigint; readonly duration: bigint; readonly maxPledge: bigint };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
  };
  "BlocksquareToken": {
    "Transfer": {
      /** The name of the event. */
      readonly eventName: "Transfer";
      /** The name of the contract that emitted this event. */
      readonly contractName: "BlocksquareToken";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly _from: Address; readonly _to: Address; readonly _amount: bigint };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
  };
  "UniswapPoolV2": {
    "Swap": {
      /** The name of the event. */
      readonly eventName: "Swap";
      /** The name of the contract that emitted this event. */
      readonly contractName: "UniswapPoolV2";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly sender: Address; readonly amount0In: bigint; readonly amount1In: bigint; readonly amount0Out: bigint; readonly amount1Out: bigint; readonly to: Address };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
    "Sync": {
      /** The name of the event. */
      readonly eventName: "Sync";
      /** The name of the contract that emitted this event. */
      readonly contractName: "UniswapPoolV2";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly reserve0: bigint; readonly reserve1: bigint };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
    "Transfer": {
      /** The name of the event. */
      readonly eventName: "Transfer";
      /** The name of the contract that emitted this event. */
      readonly contractName: "UniswapPoolV2";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly from: Address; readonly to: Address; readonly value: bigint };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
  };
  "OceanpointValuation": {
    "ValuationUpdate": {
      /** The name of the event. */
      readonly eventName: "ValuationUpdate";
      /** The name of the contract that emitted this event. */
      readonly contractName: "OceanpointValuation";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly property: Address; readonly newValuation: bigint };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
    "APYUpdate": {
      /** The name of the event. */
      readonly eventName: "APYUpdate";
      /** The name of the contract that emitted this event. */
      readonly contractName: "OceanpointValuation";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly property: Address; readonly newAPY: bigint };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
  };
  "Users": {
    "AddedWallet": {
      /** The name of the event. */
      readonly eventName: "AddedWallet";
      /** The name of the contract that emitted this event. */
      readonly contractName: "Users";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly userBytes: string; readonly wallet: Address; readonly user: string };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
    "RemovedWallet": {
      /** The name of the event. */
      readonly eventName: "RemovedWallet";
      /** The name of the contract that emitted this event. */
      readonly contractName: "Users";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly userBytes: string; readonly wallet: Address };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
  };
  "PropertyFactory": {
    "NewPropToken": {
      /** The name of the event. */
      readonly eventName: "NewPropToken";
      /** The name of the contract that emitted this event. */
      readonly contractName: "PropertyFactory";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly certifiedPartner: Address; readonly proptoken: Address; readonly createdAt: bigint };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
  };
  "CertifiedPartners": {
    "AddedCertifiedPartner": {
      /** The name of the event. */
      readonly eventName: "AddedCertifiedPartner";
      /** The name of the contract that emitted this event. */
      readonly contractName: "CertifiedPartners";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly cpBytes: string; readonly cp: string };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
    "AddedWallet": {
      /** The name of the event. */
      readonly eventName: "AddedWallet";
      /** The name of the contract that emitted this event. */
      readonly contractName: "CertifiedPartners";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly cp: string; readonly wallet: Address; readonly cpName: string };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
    "RemovedWallet": {
      /** The name of the event. */
      readonly eventName: "RemovedWallet";
      /** The name of the contract that emitted this event. */
      readonly contractName: "CertifiedPartners";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly cp: string; readonly wallet: Address };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
    "AddedWhitelisted": {
      /** The name of the event. */
      readonly eventName: "AddedWhitelisted";
      /** The name of the contract that emitted this event. */
      readonly contractName: "CertifiedPartners";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly cp: string; readonly users: readonly string[]; readonly cpName: string };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
    "RemovedWhitelisted": {
      /** The name of the event. */
      readonly eventName: "RemovedWhitelisted";
      /** The name of the contract that emitted this event. */
      readonly contractName: "CertifiedPartners";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly cp: string; readonly users: readonly string[]; readonly cpName: string };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
  };
  "DataStorageProxy": {
    "TransferPropertyToCP": {
      /** The name of the event. */
      readonly eventName: "TransferPropertyToCP";
      /** The name of the contract that emitted this event. */
      readonly contractName: "DataStorageProxy";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly property: Address; readonly cp: Address };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
  };
  "PropertyRevenueDistribution": {
    "RevenueAdded": {
      /** The name of the event. */
      readonly eventName: "RevenueAdded";
      /** The name of the contract that emitted this event. */
      readonly contractName: "PropertyRevenueDistribution";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly property: Address; readonly users: readonly Address[]; readonly amounts: readonly bigint[]; readonly fromTime: bigint; readonly toTime: bigint };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
    "RevenueClaimed": {
      /** The name of the event. */
      readonly eventName: "RevenueClaimed";
      /** The name of the contract that emitted this event. */
      readonly contractName: "PropertyRevenueDistribution";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly property: Address; readonly user: Address; readonly amount: bigint; readonly time: bigint };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
  };
  "ZeroEx": {
    "LimitOrderFilled": {
      /** The name of the event. */
      readonly eventName: "LimitOrderFilled";
      /** The name of the contract that emitted this event. */
      readonly contractName: "ZeroEx";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly orderHash: string; readonly maker: Address; readonly taker: Address; readonly feeRecipient: Address; readonly makerToken: Address; readonly takerToken: Address; readonly takerTokenFilledAmount: bigint; readonly makerTokenFilledAmount: bigint; readonly takerTokenFeeFilledAmount: bigint; readonly protocolFeePaid: bigint; readonly pool: string };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
  };
  "PropertyToken": {
    "Transfer": {
      /** The name of the event. */
      readonly eventName: "Transfer";
      /** The name of the contract that emitted this event. */
      readonly contractName: "PropertyToken";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly from: Address; readonly to: Address; readonly value: bigint };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
    "CapitalStackChange": {
      /** The name of the event. */
      readonly eventName: "CapitalStackChange";
      /** The name of the contract that emitted this event. */
      readonly contractName: "PropertyToken";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly property: Address; readonly tokenizationAmount: bigint; readonly commonEquity: bigint; readonly preferredEquity: bigint; readonly mezzanine: bigint; readonly juniorDebt: bigint; readonly seniorDebt: bigint };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
  };
  "PriceFeedRegistry": {
    "FeedConfirmed": {
      /** The name of the event. */
      readonly eventName: "FeedConfirmed";
      /** The name of the contract that emitted this event. */
      readonly contractName: "PriceFeedRegistry";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly asset: Address; readonly denomination: Address; readonly latestAggregator: Address; readonly previousAggregator: Address; readonly nextPhaseId: bigint; readonly sender: Address };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
  };
  "PropertyStakingPool": {
    "Deposit": {
      /** The name of the event. */
      readonly eventName: "Deposit";
      /** The name of the contract that emitted this event. */
      readonly contractName: "PropertyStakingPool";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly owner: Address; readonly property: Address; readonly inAmount: bigint; readonly outAmount: bigint; readonly lockedUntil: bigint };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
    "Withdraw": {
      /** The name of the event. */
      readonly eventName: "Withdraw";
      /** The name of the contract that emitted this event. */
      readonly contractName: "PropertyStakingPool";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly owner: Address; readonly property: Address; readonly inAmount: bigint; readonly outAmount: bigint; readonly rewardToUser: bigint; readonly rewardToFeeReciever: bigint; readonly isWithdraw: boolean };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
    "Reward": {
      /** The name of the event. */
      readonly eventName: "Reward";
      /** The name of the contract that emitted this event. */
      readonly contractName: "PropertyStakingPool";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly from: Address; readonly amount: bigint };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
  };
  "MarketplacePoolFactory": {
    "MarketplacePoolCreated": {
      /** The name of the event. */
      readonly eventName: "MarketplacePoolCreated";
      /** The name of the contract that emitted this event. */
      readonly contractName: "MarketplacePoolFactory";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly marketplacePoolAddress: Address; readonly cpWallet: Address; readonly bsWallet: Address; readonly cpIdentifier: string; readonly cpUrl: string; readonly tokenName: string; readonly tokenSymbol: string };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
  };
  "LiquidityStakingPool": {
    "Deposit": {
      /** The name of the event. */
      readonly eventName: "Deposit";
      /** The name of the contract that emitted this event. */
      readonly contractName: "LiquidityStakingPool";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly owner: Address; readonly inAmount: bigint; readonly outAmount: bigint; readonly lockedUntil: bigint };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
    "Withdraw": {
      /** The name of the event. */
      readonly eventName: "Withdraw";
      /** The name of the contract that emitted this event. */
      readonly contractName: "LiquidityStakingPool";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly owner: Address; readonly inAmount: bigint; readonly outAmount: bigint; readonly reward: bigint };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
    "Reward": {
      /** The name of the event. */
      readonly eventName: "Reward";
      /** The name of the contract that emitted this event. */
      readonly contractName: "LiquidityStakingPool";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly from: Address; readonly amount: bigint };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
  };
  "GovernancePool": {
    "Deposit": {
      /** The name of the event. */
      readonly eventName: "Deposit";
      /** The name of the contract that emitted this event. */
      readonly contractName: "GovernancePool";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly owner: Address; readonly inAmount: bigint; readonly outAmount: bigint };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
    "Transfer": {
      /** The name of the event. */
      readonly eventName: "Transfer";
      /** The name of the contract that emitted this event. */
      readonly contractName: "GovernancePool";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly from: Address; readonly to: Address; readonly value: bigint };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
    "Withdraw": {
      /** The name of the event. */
      readonly eventName: "Withdraw";
      /** The name of the contract that emitted this event. */
      readonly contractName: "GovernancePool";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly owner: Address; readonly inAmount: bigint; readonly outAmount: bigint };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
    "Reward": {
      /** The name of the event. */
      readonly eventName: "Reward";
      /** The name of the contract that emitted this event. */
      readonly contractName: "GovernancePool";
      /** The unique identifier of the blockchain network where this event occurred. */
      readonly chainId: 1;
      /** The parameters or arguments associated with this event. */
      readonly params: { readonly from: Address; readonly amount: bigint };
      /** The block in which this event was recorded. Configurable via `field_selection` in config.yaml. */
      readonly block: EvmBlock;
      /** The transaction that triggered this event. Configurable via `field_selection` in config.yaml. */
      readonly transaction: EvmTransaction;
      /** The index of this event's log within the block. */
      readonly logIndex: number;
      /** The address of the contract that emitted this event. */
      readonly srcAddress: Address;
    };
  };
      }; eventFilters: {
  "PropertyTokenOffering": {
    "InitialOffer": { readonly params: { readonly property?: SingleOrMultiple<Address> } };
    "Invested": { readonly params: { readonly property?: SingleOrMultiple<Address>; readonly wallet?: SingleOrMultiple<Address> } };
    "InitialOfferingCanceled": { readonly params: { readonly property?: SingleOrMultiple<Address> } };
    "ClaimInvestment": { readonly params: { readonly property?: SingleOrMultiple<Address> } };
    "ReturnedPresaleInvestment": { readonly params: { readonly property?: SingleOrMultiple<Address> } };
    "ReturnedInvestment": { readonly params: { readonly property?: SingleOrMultiple<Address> } };
  };
  "PropertyRegistry": {
    "IPFSHashChanged": { readonly params: { readonly property?: SingleOrMultiple<Address> } };
    "NameAndSymbolChange": { readonly params: { readonly property?: SingleOrMultiple<Address> } };
    "PropertyBasicInfoChanged": { readonly params: { readonly property?: SingleOrMultiple<Address> } };
    "PropertyInfoAdded": { readonly params: { readonly property?: SingleOrMultiple<Address> } };
    "PropertyInfoChanged": { readonly params: { readonly property?: SingleOrMultiple<Address> } };
    "PropertyValuationChange": { readonly params: { readonly property?: SingleOrMultiple<Address> } };
    "TokenValuationChange": { readonly params: { readonly property?: SingleOrMultiple<Address> } };
  };
  "OneInchPostInteraction": {
    "PostInteractionOrderFilled": { readonly params: { readonly orderHash?: SingleOrMultiple<string>; readonly taker?: SingleOrMultiple<Address>; readonly makingAmount?: SingleOrMultiple<bigint> } };
  };
  "PropertyTokenOfferingV2": {
    "MainSaleAdded": { readonly params: { readonly property?: SingleOrMultiple<Address> } };
    "PresaleAdded": { readonly params: { readonly property?: SingleOrMultiple<Address> } };
    "Invested": { readonly params: { readonly property?: SingleOrMultiple<Address>; readonly wallet?: SingleOrMultiple<Address> } };
    "PresaleTokensMinted": { readonly params: {} };
    "ClaimInvestment": { readonly params: { readonly property?: SingleOrMultiple<Address> } };
    "MainSaleCanceled": { readonly params: { readonly property?: SingleOrMultiple<Address> } };
    "PresaleCanceled": { readonly params: { readonly property?: SingleOrMultiple<Address> } };
    "MainSaleInvestmentsRefunded": { readonly params: { readonly property?: SingleOrMultiple<Address> } };
    "PresaleInvestmentsRefunded": { readonly params: { readonly property?: SingleOrMultiple<Address> } };
  };
  "PriceDataFeed": {
    "AnswerUpdated": { readonly params: { readonly current?: SingleOrMultiple<bigint>; readonly roundId?: SingleOrMultiple<bigint> } };
  };
  "MarketplacePool": {
    "CPInitialized": { readonly params: {} };
    "Deposit": { readonly params: { readonly owner?: SingleOrMultiple<Address> } };
    "Withdraw": { readonly params: { readonly owner?: SingleOrMultiple<Address> } };
    "CPCanWithdraw": { readonly params: {} };
    "Capped": { readonly params: {} };
    "Reward": { readonly params: { readonly from?: SingleOrMultiple<Address> } };
    "LockExtended": { readonly params: {} };
    "LiquidateCPCollateral": { readonly params: {} };
    "PoolCampaignConfigured": { readonly params: {} };
  };
  "BlocksquareToken": {
    "Transfer": { readonly params: { readonly _from?: SingleOrMultiple<Address>; readonly _to?: SingleOrMultiple<Address> } };
  };
  "UniswapPoolV2": {
    "Swap": { readonly params: { readonly sender?: SingleOrMultiple<Address>; readonly to?: SingleOrMultiple<Address> } };
    "Sync": { readonly params: {} };
    "Transfer": { readonly params: { readonly from?: SingleOrMultiple<Address>; readonly to?: SingleOrMultiple<Address> } };
  };
  "OceanpointValuation": {
    "ValuationUpdate": { readonly params: {} };
    "APYUpdate": { readonly params: {} };
  };
  "Users": {
    "AddedWallet": { readonly params: { readonly userBytes?: SingleOrMultiple<string> } };
    "RemovedWallet": { readonly params: { readonly userBytes?: SingleOrMultiple<string> } };
  };
  "PropertyFactory": {
    "NewPropToken": { readonly params: { readonly certifiedPartner?: SingleOrMultiple<Address>; readonly proptoken?: SingleOrMultiple<Address> } };
  };
  "CertifiedPartners": {
    "AddedCertifiedPartner": { readonly params: { readonly cpBytes?: SingleOrMultiple<string> } };
    "AddedWallet": { readonly params: { readonly cp?: SingleOrMultiple<string> } };
    "RemovedWallet": { readonly params: { readonly cp?: SingleOrMultiple<string> } };
    "AddedWhitelisted": { readonly params: { readonly cp?: SingleOrMultiple<string> } };
    "RemovedWhitelisted": { readonly params: { readonly cp?: SingleOrMultiple<string> } };
  };
  "DataStorageProxy": {
    "TransferPropertyToCP": { readonly params: {} };
  };
  "PropertyRevenueDistribution": {
    "RevenueAdded": { readonly params: { readonly property?: SingleOrMultiple<Address> } };
    "RevenueClaimed": { readonly params: { readonly property?: SingleOrMultiple<Address>; readonly user?: SingleOrMultiple<Address> } };
  };
  "ZeroEx": {
    "LimitOrderFilled": { readonly params: {} };
  };
  "PropertyToken": {
    "Transfer": { readonly params: { readonly from?: SingleOrMultiple<Address>; readonly to?: SingleOrMultiple<Address> } };
    "CapitalStackChange": { readonly params: { readonly property?: SingleOrMultiple<Address> } };
  };
  "PriceFeedRegistry": {
    "FeedConfirmed": { readonly params: { readonly asset?: SingleOrMultiple<Address>; readonly denomination?: SingleOrMultiple<Address>; readonly latestAggregator?: SingleOrMultiple<Address> } };
  };
  "PropertyStakingPool": {
    "Deposit": { readonly params: { readonly owner?: SingleOrMultiple<Address> } };
    "Withdraw": { readonly params: { readonly owner?: SingleOrMultiple<Address> } };
    "Reward": { readonly params: { readonly from?: SingleOrMultiple<Address> } };
  };
  "MarketplacePoolFactory": {
    "MarketplacePoolCreated": { readonly params: { readonly marketplacePoolAddress?: SingleOrMultiple<Address> } };
  };
  "LiquidityStakingPool": {
    "Deposit": { readonly params: { readonly owner?: SingleOrMultiple<Address> } };
    "Withdraw": { readonly params: { readonly owner?: SingleOrMultiple<Address> } };
    "Reward": { readonly params: { readonly from?: SingleOrMultiple<Address> } };
  };
  "GovernancePool": {
    "Deposit": { readonly params: { readonly owner?: SingleOrMultiple<Address> } };
    "Transfer": { readonly params: { readonly from?: SingleOrMultiple<Address>; readonly to?: SingleOrMultiple<Address> } };
    "Withdraw": { readonly params: { readonly owner?: SingleOrMultiple<Address> } };
    "Reward": { readonly params: { readonly from?: SingleOrMultiple<Address> } };
  };
      } };
      entities: Entities;
      enums: Enums;
    };
  }

  export type AssetPair = Entities["AssetPair"];
  export type AssetPairPrice = Entities["AssetPairPrice"];
  export type CertifiedPartner = Entities["CertifiedPartner"];
  export type Global = Entities["Global"];
  export type GlobalRecord = Entities["GlobalRecord"];
  export type MainSaleInvestment = Entities["MainSaleInvestment"];
  export type MainSaleOffering = Entities["MainSaleOffering"];
  export type MarketplacePool = Entities["MarketplacePool"];
  export type MarketplacePoolPosition = Entities["MarketplacePoolPosition"];
  export type MarketplacePoolRecord = Entities["MarketplacePoolRecord"];
  export type MarketplacePoolTransaction = Entities["MarketplacePoolTransaction"];
  export type OceanpointTokenInformation = Entities["OceanpointTokenInformation"];
  export type PresaleOffering = Entities["PresaleOffering"];
  export type PropertyStakingPool = Entities["PropertyStakingPool"];
  export type PropertyStakingPoolPosition = Entities["PropertyStakingPoolPosition"];
  export type PropertyStakingPoolRecord = Entities["PropertyStakingPoolRecord"];
  export type PropertyStakingPoolTransaction = Entities["PropertyStakingPoolTransaction"];
  export type PropertyToken = Entities["PropertyToken"];
  export type PropertyTokenCapitalStack = Entities["PropertyTokenCapitalStack"];
  export type PropertyTokenHolder = Entities["PropertyTokenHolder"];
  export type PropertyTokenInvestment = Entities["PropertyTokenInvestment"];
  export type PropertyTokenOffering = Entities["PropertyTokenOffering"];
  export type PropertyTokenOfferingV2 = Entities["PropertyTokenOfferingV2"];
  export type PropertyTokenRecord = Entities["PropertyTokenRecord"];
  export type PropertyTokenRevenue = Entities["PropertyTokenRevenue"];
  export type PropertyTokenRevenueClaim = Entities["PropertyTokenRevenueClaim"];
  export type PropertyTokenRevenueDistribution = Entities["PropertyTokenRevenueDistribution"];
  export type PropertyTokenTrade = Entities["PropertyTokenTrade"];
  export type PropertyTokenTradeCounts = Entities["PropertyTokenTradeCounts"];
  export type PropertyTokenTransfer = Entities["PropertyTokenTransfer"];
  export type Referral = Entities["Referral"];
  export type Reward = Entities["Reward"];
  export type StakingPool = Entities["StakingPool"];
  export type StakingPoolPosition = Entities["StakingPoolPosition"];
  export type StakingPoolPositionRecord = Entities["StakingPoolPositionRecord"];
  export type StakingPoolRecord = Entities["StakingPoolRecord"];
  export type StakingPoolTransaction = Entities["StakingPoolTransaction"];
  export type Token = Entities["Token"];
  export type TokenDeposit = Entities["TokenDeposit"];
  export type TokenHolder = Entities["TokenHolder"];
  export type TokenHolderRecord = Entities["TokenHolderRecord"];
  export type TokenRecord = Entities["TokenRecord"];
  export type UniswapV2Pool = Entities["UniswapV2Pool"];
  export type User = Entities["User"];
  export type UserCertifiedPartner = Entities["UserCertifiedPartner"];
  export type Wallet = Entities["Wallet"];
}
